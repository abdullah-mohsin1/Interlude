# Interlude

## Environment setup

Create a local `.env` in the repo root with:

- `GRADIUM_API_KEY=PASTE_NEW_KEY_HERE`
- `GRADIUM_VOICE_ID=zVI-68f2GRJbOGTT`
- `GRADIUM_REGION=us`

For production, set these in your hosting provider environment variables and do not rely on `.env`.
